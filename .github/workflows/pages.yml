name: Deploy Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Terminal casts and site/presentation/ are generated locally (python build_site.py)
      # and committed; deploy only substitutes version placeholders and uploads ./site.

      - name: Inject version and build date into site
        run: |
          VERSION=$(grep '^version' pyproject.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          BUILD_DATE=$(date +"%Y-%m-%d")
          echo "version: $VERSION  date: $BUILD_DATE"
          find site -name '*.html' -exec sed -i "s/__BUILD_VERSION__/$VERSION/g" {} +
          find site -name '*.html' -exec sed -i "s/__BUILD_DATE__/$BUILD_DATE/g" {} +

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./site

      - id: deployment
        uses: actions/deploy-pages@v4
